@model IEnumerable<Smart_Library_Management_System.Models.Borrow>
@{
    ViewData["Title"] = "Borrows";
}
<h2 class="mb-3">📖 Borrow List</h2>
<a asp-action="Create" class="btn btn-primary mb-3">
    ➕ New Borrow
</a>
<table class="table table-bordered table-striped">
    <thead class="table-dark">
        <tr>
            <th>Borrower</th>
            <th>Date</th>
            <th>Return Date</th>
            <th>Books</th>
            <th>Total Items</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var borrow in Model)
        {
            <tr>
                <td>@borrow.BorrowName</td>
                <td>@borrow.BorrowDate.ToString("yyyy-MM-dd HH:mm")</td>
                <td>
                    @(borrow.ReturnDate.HasValue
                        ? borrow.ReturnDate.Value.ToString("yyyy-MM-dd")
                        : "Not returned")
                </td>

                <td>
                    @if (borrow.BorrowItems != null && borrow.BorrowItems.Any())
                    {
                        <ul class="mb-0">
                            @foreach (var item in borrow.BorrowItems)
                            {
                                <li>@item.Book?.Title (x @item.Quantity)</li>
                            }
                        </ul>
                    }
                    else
                    {
                        <span class="text-muted">No items</span>
                    }
                </td>

                <td>@borrow.BorrowItems?.Sum(i => i.Quantity)</td>

                <!-- ✅ Tout dans le même <td> Actions -->
                <td>
                    @if (borrow.ReturnDate == null)
                    {
                        <a asp-action="Return" asp-route-id="@borrow.BorrowID"
                           class="btn btn-sm btn-success me-1">
                            ↩️ Return
                        </a>
                    }
                    else
                    {
                        <span class="badge bg-secondary">
                           Returned @borrow.ReturnDate?.ToString("dd/MM/yyyy")
                        </span>
                    }
                </td>

            </tr>
        }
    </tbody>
</table>